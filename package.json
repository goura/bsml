{
  "name": "bsml-renderer",
  "version": "0.1.0",
  "description": "BSML (Balance Sheet Modeling Language) parser and renderer",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "chevrotain": "^11.0.0"
  }
}